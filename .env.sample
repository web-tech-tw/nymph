# http
BASE_URL=""
HTTP_PORT="3000"

# database
MONGODB_URI="mongodb://localhost:27017/nymph"

# nvidia nim integration
NIM_API_KEY="your_nim_api_key"
NIM_BASE_URL="https://integrate.api.nvidia.com/v1"
NIM_MODEL="openai/gpt-oss-120b"

# discord integration
DISCORD_BOT_TOKEN="your_discord_bot_token"

# line integration
LINE_CHANNEL_ACCESS_TOKEN="your_line_channel_access_token"
LINE_CHANNEL_SECRET="your_line_channel_secret"

# etl integration
SOURCE_MONGODB_URI="mongodb+srv://<username>:<password>@<host>/openchat?retryWrites=true&w=majority"
SOURCE_MONGODB_DB_NAME="openchat"
SOURCE_MONGODB_COLLECTION="messages"
ETL_NIM_API_KEY="your_etl_nim_api_key"
ETL_NIM_BASE_URL="https://integrate.api.nvidia.com/v1"
ETL_NIM_MODEL="openai/gpt-oss-120b"

# mcp integration (Model Context Protocol)
# MCP configuration is loaded from a TOML file (default: `./mcp.toml`).
# Copy `mcp.toml.sample` to `mcp.toml` to configure MCP servers.
# MCP_CONFIG_PATH="./mcp.toml"

# sara identity integration
SARA_INTE_HOST="https://web-tech.tw/sara"
SARA_RECV_HOST="https://web-tech.tw/recv/sara"
